---
title: "Models"
description: "GET /v1/models — list models available through Toolken."
---

`GET https://gateway.toolken.ai/v1/models`

Returns a list of models recognized by Toolken for routing. Include `X-Toolken-Key` for gateway authentication. The response mirrors the OpenAI models list format.

<Note>This page is a stub — the full reference is coming soon.</Note>
