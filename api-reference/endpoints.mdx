---
title: "Endpoints"
description: "All supported gateway endpoints with request and response shapes."
---

The Toolken gateway exposes the following endpoints:

| Method | Path | Description |
|--------|------|-------------|
| `POST` | `/v1/chat/completions` | OpenAI-compatible chat completions (streaming and non-streaming) |
| `POST` | `/v1/messages` | Anthropic-compatible messages endpoint |
| `GET` | `/v1/models` | List available models |

All endpoints forward the request body and upstream provider headers verbatim, except for `X-Toolken-Key` which is stripped before forwarding.

<Note>This page is a stub — the full guide is coming soon.</Note>
