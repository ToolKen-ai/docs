---
title: "Attribution Recipes"
description: "Copy-paste patterns for common attribution scenarios."
---

Common recipes include tagging calls by customer tier, separating development from production traffic via the `environment` key, and combining `feature` + `customer_id` for per-customer-per-feature cost breakdown. Each recipe shows the header values and the resulting dashboard view.

<Note>This page is a stub — the full guide is coming soon.</Note>
